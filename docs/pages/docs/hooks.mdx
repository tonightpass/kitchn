---
title: "Custom React hooks for enhanced functionality"
description: "A collection of custom hooks that can be used to enhance the functionality of your React website or web application."
---

import { Card, Cards } from 'nextra-theme-docs'
import hooksMetadata from './hooks/_meta.json'

# Hooks

Kitchn provides a variety of custom hooks to simplify and enhance the functionality of your projects. Below is a list of all available hooks:

<Cards>
  {Object.entries(hooksMetadata).map(([key, value]) => (
    <Card key={key} title={value} href={`/docs/hooks/${key}`} />
  ))}
</Cards>
