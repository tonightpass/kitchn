---
title: Create React App or Vite React
description: A guide for installing Kitchen with Create React App or Vite React projects
tags: ['create-react-app', 'vite', 'react', 'typescript', 'javascript', 'frameworks', 'getting-started']
category: frameworks
---

import { Tab, Tabs } from "nextra-theme-docs";

# Getting Started with Create React App or Vite React

## Installation
In your Create React App or Vite React project, install Kitchen by running either of the following:

```sh npm2yarn
npm i @tonightpass/kitchen --save
```

## Provider Setup

After installing Kitchen, you need to set up the `KitchenProvider` at the root 
of your application. This can be either in your `App.jsx` or `App.tsx` file.

```jsx
// App.jsx
import { KitchenProvider } from "@tonightpass/kitchen";

export default function App({ Component, pageProps }) {
  return (
    <KitchenProvider>
      <TheRestOfYourApplication />
    </KitchenProvider>
  );
}
```

## Deploy your own

Deploy the example using [Vercel](https://vercel.com/new/clone?repository-url=https://github.com/tonightpass/kitchen/tree/master/examples/react-typescript&project-name=kitchen-react&repository-name=kitchen-react) or preview live with [StackBlitz](https://stackblitz.com/github/tonightpass/kitchen/tree/master/examples/react-typescript) or [CodeSandbox](https://codesandbox.io/s/github/tonightpass/kitchen/tree/master/examples/react-typescript).

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/tonightpass/kitchen/tree/master/examples/react-typescript&project-name=kitchen-react&repository-name=kitchen-react)

In addition, here is a complete [project example](https://github.com/tonightpass/kitchen/tree/master/examples/react-typescript) using Kitchen with Next.js.
