---
title: "React components for building websites and applications"
description: "A collection of components that can be used to build a React website or web application."
---

import { Card, Cards } from 'nextra-theme-docs'
import componentsMetadata from './components/_meta.json'

# Components

Kitchn provides prebuild components to help you build your projects faster. Here is an overview of all the components that are available:

<Cards>
  {Object.entries(componentsMetadata).map(([key, value]) => (
    <Card key={key} title={value} href={`/docs/components/${key}`} />
  ))}
</Cards>
